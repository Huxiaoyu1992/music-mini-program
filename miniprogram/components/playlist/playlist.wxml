<view class="play-item" bind:tap="getMusiclist">
  <image src="{{playlist.picUrl}}" class="pic"></image>
  <text class="play-count">{{_count}}</text>
  <view class="play-name">{{playlist.name}}</view>
</view>